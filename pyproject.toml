[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "pitwall-agent"
version = "0.4.0a2"
description = "Open-source endurance stint planner with deterministic fuel, tyre, driver-rotation, and pit-stop strategy."
readme = "README.md"
requires-python = ">=3.11,<3.15"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Sreenath R." }]
keywords = [
  "endurance-racing",
  "fuel-strategy",
  "motorsport",
  "ollama",
  "pit-stop-strategy",
  "race-strategy",
  "sim-racing",
  "stint-planner",
  "telemetry",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: End Users/Desktop",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
]
dependencies = [
  "rich>=13.9,<16",
  "typer>=0.12,<1",
]

[project.optional-dependencies]
dev = [
  "build>=1.5.0,<2",
  "pytest>=9.1.1,<10",
  "pytest-cov>=7.1.0,<8",
  "ruff>=0.16.0,<1",
]

[project.scripts]
pitwall = "pitwall.cli:app"

[project.urls]
Documentation = "https://dabi-init.github.io/endurance-stint-planner/"
Repository = "https://github.com/Dabi-init/endurance-stint-planner"
Issues = "https://github.com/Dabi-init/endurance-stint-planner/issues"
Changelog = "https://github.com/Dabi-init/endurance-stint-planner/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
include = ["engine*", "pitwall*"]

[tool.setuptools.package-data]
"pitwall.presets" = ["*.json"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["engine", "pitwall"]
omit = [
  "pitwall/__main__.py",
  "pitwall/cli.py",
]

[tool.coverage.report]
fail_under = 85
show_missing = true
skip_covered = true

[tool.ruff]
target-version = "py311"
line-length = 88
extend-exclude = [".venv"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "RUF"]
ignore = ["RUF001"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
