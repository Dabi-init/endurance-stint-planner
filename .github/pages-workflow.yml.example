name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths: ['docs/**', '.github/workflows/pages.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy docs/
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v4
      - name: Upload docs/ artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# NOTE
# This is an optional template, not the repository's active Pages configuration.
# Pages currently serves main:/docs directly. Use this only if a future site
# build requires switching Settings -> Pages -> Source to "GitHub Actions":
#   1. Copy this file to .github/workflows/pages.yml (remove this NOTE block).
#   2. Repository Settings -> Pages -> Source: "GitHub Actions".
#   3. Actions -> "Deploy GitHub Pages" -> Run workflow.
